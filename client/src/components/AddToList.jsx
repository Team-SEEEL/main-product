import React from 'react';

const AddToList = (props) => {

    return (
        <select >
            <option value="Create New List">Add To List</option>
        </select>
    )
}

export default AddToList;